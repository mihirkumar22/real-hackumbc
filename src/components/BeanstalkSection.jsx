import React, { useRef, useEffect } from "react";

export default function BeanstalkSection() {
    const contentRef = useRef(null);

    return (
        <div style={{
            width: '100%',
            height: '500px',
            backgroundColor: 'blue',
        }}>

        </div>
    )
}